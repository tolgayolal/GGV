import { Color } from "./Cases";

export interface ILiveItem {
      item: string;
      collection: string;
      color: string;
      owner: string;
      caseName: string;
      casePhoto:string
      itemPhoto: string;
}